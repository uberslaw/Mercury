<Application x:Class="Mercury.Catcher.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <SolidColorBrush x:Key="BgBrush" Color="#fff5e3"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#ffe7cb"/>
        <SolidColorBrush x:Key="HeaderBrush" Color="#f4c2c1"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#fadadd"/>
        <SolidColorBrush x:Key="TextBrush" Color="#4a2f2c"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#8c5e5a"/>
        <SolidColorBrush x:Key="OnHeaderBrush" Color="#4a2f2c"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#f4c2c1"/>
        <SolidColorBrush x:Key="ButtonBrush" Color="#ffdab8"/>
        <SolidColorBrush x:Key="ButtonForegroundBrush" Color="#4a2f2c"/>
        <SolidColorBrush x:Key="AccentForegroundBrush" Color="#4a2f2c"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#7a3b3b"/>
        <SolidColorBrush x:Key="WarnBrush" Color="#9a5348"/>
        <SolidColorBrush x:Key="OkBrush" Color="#3f5c3a"/>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#ffe7cb"/>
        <SolidColorBrush x:Key="StatusBarBrush" Color="#f4c2c1"/>
        <SolidColorBrush x:Key="StatusBarTextBrush" Color="#4a2f2c"/>
        <SolidColorBrush x:Key="CancelButtonBrush" Color="#fadadd"/>

        <Style TargetType="Window">
            <Setter Property="Icon" Value="pack://application:,,,/Assets/Mercury.ico"/>
            <Setter Property="Background" Value="{DynamicResource BgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
        </Style>
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="88"/>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Background" Value="{DynamicResource CancelButtonBrush}"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
        </Style>
        <Style TargetType="PasswordBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="{DynamicResource StatusBarBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource StatusBarTextBrush}"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>
    </Application.Resources>
</Application>
