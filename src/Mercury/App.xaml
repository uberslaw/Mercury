<Application x:Class="Mercury.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Mercury"
             xmlns:sys="clr-namespace:System;assembly=System.Runtime"
             xmlns:po="http://schemas.microsoft.com/winfx/2006/xaml/presentation/options"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="po"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <local:InverseBoolConverter x:Key="InverseBool"/>
        <local:InverseBoolToVisConverter x:Key="InverseBoolToVis"/>

        <!-- Warm chrome + dark warm text: cream/peach/rose surfaces, #4a2f2c body text -->
        <SolidColorBrush x:Key="BgBrush" Color="#fff5e3" po:Freeze="False"/>
        <SolidColorBrush x:Key="PanelBrush" Color="#ffe7cb" po:Freeze="False"/>
        <SolidColorBrush x:Key="PanelAltBrush" Color="#ffe7cb" po:Freeze="False"/>
        <SolidColorBrush x:Key="HeaderBrush" Color="#f4c2c1" po:Freeze="False"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#fadadd" po:Freeze="False"/>
        <SolidColorBrush x:Key="TextBrush" Color="#4a2f2c" po:Freeze="False"/>
        <SolidColorBrush x:Key="MutedBrush" Color="#8c5e5a" po:Freeze="False"/>
        <SolidColorBrush x:Key="OnHeaderBrush" Color="#4a2f2c" po:Freeze="False"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#f4c2c1" po:Freeze="False"/>
        <SolidColorBrush x:Key="BarTrackBrush" Color="#ffdab8" po:Freeze="False"/>
        <SolidColorBrush x:Key="BarFillBrush" Color="#f4c2c1" po:Freeze="False"/>
        <SolidColorBrush x:Key="HeaderBarTrackBrush" Color="#ffdab8" po:Freeze="False"/>
        <SolidColorBrush x:Key="HeaderBarFillBrush" Color="#fff5e3" po:Freeze="False"/>
        <SolidColorBrush x:Key="ButtonBrush" Color="#ffdab8" po:Freeze="False"/>
        <SolidColorBrush x:Key="ButtonHoverBrush" Color="#ffdab8" po:Freeze="False"/>
        <SolidColorBrush x:Key="ButtonForegroundBrush" Color="#4a2f2c" po:Freeze="False"/>
        <SolidColorBrush x:Key="AccentForegroundBrush" Color="#4a2f2c" po:Freeze="False"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#7a3b3b" po:Freeze="False"/>
        <SolidColorBrush x:Key="WarnBrush" Color="#9a5348" po:Freeze="False"/>
        <SolidColorBrush x:Key="OkBrush" Color="#3f5c3a" po:Freeze="False"/>
        <SolidColorBrush x:Key="ErrorLineBrush" Color="#7a3b3b" po:Freeze="False"/>
        <SolidColorBrush x:Key="StatusBarBrush" Color="#f4c2c1" po:Freeze="False"/>
        <SolidColorBrush x:Key="StatusBarTextBrush" Color="#4a2f2c" po:Freeze="False"/>
        <SolidColorBrush x:Key="CancelButtonBrush" Color="#fadadd" po:Freeze="False"/>
        <SolidColorBrush x:Key="InputBackgroundBrush" Color="#ffe7cb" po:Freeze="False"/>
        <SolidColorBrush x:Key="TabBackgroundBrush" Color="#ffe7cb" po:Freeze="False"/>
        <SolidColorBrush x:Key="TabSelectedBackgroundBrush" Color="#fff5e3" po:Freeze="False"/>
        <SolidColorBrush x:Key="TabSelectedForegroundBrush" Color="#4a2f2c" po:Freeze="False"/>
        <SolidColorBrush x:Key="TabUnselectedForegroundBrush" Color="#8c5e5a" po:Freeze="False"/>
        <SolidColorBrush x:Key="ConsoleBackgroundBrush" Color="#ffe7cb" po:Freeze="False"/>
        <SolidColorBrush x:Key="QueueBackgroundBrush" Color="#ffe7cb" po:Freeze="False"/>

        <FontFamily x:Key="BodyFontFamily">Segoe UI</FontFamily>
        <sys:Double x:Key="BodyFontSize">13</sys:Double>
        <FontFamily x:Key="ProgressFontFamily">Segoe UI</FontFamily>
        <sys:Double x:Key="ProgressFontSize">15</sys:Double>
        <FontFamily x:Key="LabelFontFamily">Segoe UI</FontFamily>
        <sys:Double x:Key="LabelFontSize">13</sys:Double>
        <FontFamily x:Key="ValueFontFamily">Segoe UI</FontFamily>
        <sys:Double x:Key="ValueFontSize">13</sys:Double>
        <FontFamily x:Key="ButtonFontFamily">Segoe UI</FontFamily>
        <sys:Double x:Key="ButtonFontSize">13</sys:Double>
        <FontFamily x:Key="ConsoleFontFamily">Consolas</FontFamily>
        <sys:Double x:Key="ConsoleFontSize">12</sys:Double>

        <Style x:Key="ChromeHighlight" TargetType="Border">
            <Setter Property="BorderThickness" Value="3"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
        </Style>
        <Style x:Key="SwatchButton" TargetType="Button">
            <Setter Property="Width" Value="36"/>
            <Setter Property="Height" Value="26"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Window">
            <Setter Property="Icon" Value="pack://application:,,,/Mercury;component/Assets/Mercury.ico"/>
            <Setter Property="Background" Value="{DynamicResource BgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="FontFamily" Value="{DynamicResource BodyFontFamily}"/>
            <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}"/>
        </Style>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="Padding" Value="0,0,8,0"/>
        </Style>
        <Style x:Key="ToolbarButton" TargetType="Button">
            <Setter Property="Background" Value="{DynamicResource ButtonBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource ButtonForegroundBrush}"/>
            <Setter Property="FontFamily" Value="{DynamicResource ButtonFontFamily}"/>
            <Setter Property="FontSize" Value="{DynamicResource ButtonFontSize}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="88"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource ButtonHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="CompactButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>
        <Style x:Key="HeaderButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Background" Value="{DynamicResource HeaderBarTrackBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource OnHeaderBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource OnHeaderBrush}"/>
            <Setter Property="Margin" Value="8,0,0,0"/>
        </Style>
        <Style TargetType="Menu">
            <Setter Property="Background" Value="{DynamicResource HeaderBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource OnHeaderBrush}"/>
        </Style>
        <Style TargetType="MenuItem">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource BgBrush}"/>
        </Style>
        <Style TargetType="ContextMenu">
            <Setter Property="Background" Value="{DynamicResource BgBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
        </Style>
        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
        <Style x:Key="CancelButton" TargetType="Button" BasedOn="{StaticResource ToolbarButton}">
            <Setter Property="Background" Value="{DynamicResource CancelButtonBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}"/>
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="CaretBrush" Value="{DynamicResource TextBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ThemeHexBox" TargetType="TextBox">
            <Setter Property="Foreground" Value="#4A2F2C"/>
            <Setter Property="CaretBrush" Value="#4A2F2C"/>
            <Setter Property="Background" Value="#FFF5E3"/>
            <Setter Property="BorderBrush" Value="#C4A484"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>
        <Style x:Key="ThemeSwatchButton" TargetType="Button" BasedOn="{StaticResource SwatchButton}">
            <Setter Property="BorderBrush" Value="#C4A484"/>
        </Style>
        <Style TargetType="CheckBox">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Margin" Value="0,4,16,4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style TargetType="ComboBox">
            <Setter Property="Background" Value="{DynamicResource InputBackgroundBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="MinHeight" Value="28"/>
        </Style>
        <Style TargetType="TabControl">
            <Setter Property="Background" Value="{DynamicResource PanelBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
        </Style>
        <Style TargetType="TabItem">
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="Background" Value="{DynamicResource TabBackgroundBrush}"/>
            <Setter Property="Padding" Value="16,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Border x:Name="bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{DynamicResource BorderBrush}"
                                BorderThickness="1,1,1,0"
                                Padding="{TemplateBinding Padding}"
                                Margin="2,2,2,0"
                                CornerRadius="4,4,0,0">
                            <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="{DynamicResource TabSelectedBackgroundBrush}"/>
                                <Setter Property="Foreground" Value="{DynamicResource TabSelectedForegroundBrush}"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="{DynamicResource TabUnselectedForegroundBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ProgressBar">
            <Setter Property="Height" Value="14"/>
            <Setter Property="Foreground" Value="{DynamicResource BarFillBrush}"/>
            <Setter Property="Background" Value="{DynamicResource BarTrackBrush}"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
        <Style TargetType="StatusBar">
            <Setter Property="Background" Value="{DynamicResource StatusBarBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource StatusBarTextBrush}"/>
            <Setter Property="Padding" Value="10,6"/>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{DynamicResource PanelAltBrush}"/>
            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="FontFamily" Value="{DynamicResource ConsoleFontFamily}"/>
            <Setter Property="FontSize" Value="{DynamicResource ConsoleFontSize}"/>
        </Style>
        <Style TargetType="GroupBox">
            <Setter Property="Foreground" Value="{DynamicResource MutedBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,0,0,12"/>
        </Style>
    </Application.Resources>
</Application>
