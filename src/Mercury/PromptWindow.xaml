<Window x:Class="Mercury.PromptWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save job"
        Width="460" Height="170"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize">
    <DockPanel Margin="16">
        <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
            <Button Content="Cancel" Style="{StaticResource ToolbarButton}" IsCancel="True" Click="Cancel_Click"/>
            <Button Content="Save" Style="{StaticResource AccentButton}" IsDefault="True" Click="Ok_Click" Margin="0"/>
        </StackPanel>
        <StackPanel>
            <TextBlock x:Name="PromptText" Text="Name" Margin="0,0,0,8"/>
            <TextBox x:Name="ValueBox"/>
        </StackPanel>
    </DockPanel>
</Window>
